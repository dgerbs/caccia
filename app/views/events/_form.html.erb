<%= simple_form_for(@event) do |f| %>

<%= f.input :title, placeholder: "Title" %>
<%= f.input :location, placeholder: "Location" %>
<%= f.input :summary, placeholder: "Summary" %>

<div class="form-actions">
  <%= f.button :submit %>
</div>
<% end %>